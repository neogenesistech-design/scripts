// ========================================================================
// SCRIPT DEFINITIVO: SMART SCANNER V3 (UI OTIMIZADA + CRASH LOGGER)
// ========================================================================

function deleteCacheFiles() {
    Java.perform(() => {
        try {
            const ActivityThread = Java.use("android.app.ActivityThread");
            const context = ActivityThread.currentApplication().getApplicationContext();
            const pkgName = context.getPackageName();
            const File = Java.use("java.io.File");
            const path = Java.use("android.os.Environment").getExternalStorageDirectory().getAbsolutePath() + "/Android/data/" + pkgName + "/";
            // Preserva os arquivos de log de crash
            const EXCLUDE_PATHS = ["files/levels/images/classiciphone", "saved_functions.txt", "alvo_atual.txt", "blacklist_crashes.txt"];
            const targetDir = File.$new(path);
            if (!targetDir.exists() || !targetDir.isDirectory()) return;
            const files = targetDir.listFiles();
            for (let i = 0; i < files.length; i++) {
                try {
                    const f = files[i];
                    let exclude = false;
                    for (let k = 0; k < EXCLUDE_PATHS.length; k++) if (f.getAbsolutePath().includes(EXCLUDE_PATHS[k])) exclude = true;
                    if (exclude) continue;
                    if (f.isDirectory()) deleteRecursive(f, EXCLUDE_PATHS); else f.delete();
                } catch (err) {}
            }
        } catch (e) {}
    });
}
function deleteRecursive(f, e) { try { const sub = f.listFiles(); if (sub) for (let j = 0; j < sub.length; j++) { let ex = false; for (let k = 0; k < e.length; k++) if (sub[j].getAbsolutePath().includes(e[k])) ex = true; if (ex) continue; if (sub[j].isDirectory()) deleteRecursive(sub[j], e); else sub[j].delete(); } f.delete(); } catch (err) {} }
deleteCacheFiles();

setTimeout(() => {
    // --- FUNÇÕES DE SEGURANÇA ---
    function getSafeNativeFunc(lib, funcName, retType, args) {
        const ptr = Module.findExportByName(lib, funcName);
        if (ptr) return new NativeFunction(ptr, retType, args);
        return null;
    }
    
    var malloc = getSafeNativeFunc("libc.so", "malloc", "pointer", ["int"]);

    // --- VARIÁVEIS GLOBAIS ---
    let unhit = false, unhit_mode = 1, showAdmins = true, autoOFF = false, afkInterval = null;
    let autoOffHasTriggered = false, wallhack = false, canExplode = false, explosionsInterval = null;
    let explosionRadius = 90, lastAdminToastTime = 0, lastAdminSeenTime = 0; 
    let autoOffButtons = []; 

    let pendingAdminDisable = false;
    let pendingAdminToastMsg = null;

    let scanMode = 0; 
    let blacklistedFunctions = new Set(); 
    let smartListLogs = []; 
    let interceptedData = {}; 
    let selectedTargetName = null; 
    let saveCounter = 1; 
    let uiNeedsUpdate = false; 

    // --- VARIÁVEIS SPAM PROJÉTIL ---
    let isSpamming = false;
    let spamSpeeds = [250, 500, 1000, 2000]; // em ms
    let spamSpeedIdx = 1; // Padrão: 500ms
    let spamIntervalId = null;

    // --- VARIÁVEIS ÂNCORA ---
    let currentScanHooks = []; // Guarda os hooks do scanner para podermos trocar de âncora
    const anchorOptions = [
        { name: "HurtPlayer (Dano)", ref: "_ZN10W6NzgawMJy10iPzUgaQKcQEdddRK10C8THgaTQxFP10MpGzgariDy" },
        { name: "Invocar Projétil", ref: "_ZN10W6NzgawMJy10UwflwasGuPEv" },
        { name: "Arremessar Item", ref: "_ZN10W6NzgawMJy10IFrkwaQTOOEv" },
        { name: "Heal (Vida)", ref: "_ZN10W6NzgawMJy10zwkXwa7SwkEd" }
    ];
    let currentAnchorRef = anchorOptions[0].ref;

    // --- PONTEIROS ESSENCIAIS ---
    const activeplayerPtr = Module.findExportByName("libqplay.so", "dkCHgaGRiF");
    function getActivePlayer() { return activeplayerPtr ? activeplayerPtr.readPointer() : ptr(0); }

    const setVarValue = getSafeNativeFunc("libqplay.so", "_ZN10G0gxgajWBw10gVHMMaRyAEERK10C8THgaTQxF", "void", ["pointer", "pointer"]);
    const getlocaly = getSafeNativeFunc("libqplay.so", "_ZNK10W6NzgawMJy10qCgWga1ADREv", "double", ["pointer"]);
    const getlocalx = getSafeNativeFunc("libqplay.so", "_ZNK10W6NzgawMJy10Qi2VgaCyrREv", "double", ["pointer"]);
    const putExplosion = getSafeNativeFunc("libqplay.so", "_ZN10zF9VgaBKxR10tMaombuGXrEdddii", "void", ["pointer", "double", "double", "double", "int", "int"]);

    const forceHeal = getSafeNativeFunc("libqplay.so", "_ZN10W6NzgawMJy10zwkXwa7SwkEd", "void", ["pointer", "double"]);
    const forceThrow = getSafeNativeFunc("libqplay.so", "_ZN10W6NzgawMJy10IFrkwaQTOOEv", "void", ["pointer"]);
    const forceProjectile = getSafeNativeFunc("libqplay.so", "_ZN10W6NzgawMJy10UwflwasGuPEv", "void", ["pointer"]);

    const zoomAdminStr = createTString('zoom=1.8'); // Admin Gigante

    let refreshScannerUI = null; 
    let refreshSpamUI = null;

    // --- SISTEMA DE LOG DE CRASH PERSISTENTE ---
    function checkPreviousCrash(mainActivity) {
        try {
            const context = mainActivity.getApplicationContext();
            const basePath = Java.use("android.os.Environment").getExternalStorageDirectory().getAbsolutePath() + "/Android/data/" + context.getPackageName() + "/";
            const File = Java.use("java.io.File");
            const FileReader = Java.use("java.io.FileReader");
            const BufferedReader = Java.use("java.io.BufferedReader");
            const FileWriter = Java.use("java.io.FileWriter");
            const BufferedWriter = Java.use("java.io.BufferedWriter");
            const JString = Java.use("java.lang.String");

            const fileTarget = File.$new(basePath + "alvo_atual.txt");
            const fileBlacklist = File.$new(basePath + "blacklist_crashes.txt");

            if (fileBlacklist.exists()) {
                const br = BufferedReader.$new(FileReader.$new(fileBlacklist));
                let line;
                while ((line = br.readLine()) !== null) { blacklistedFunctions.add(line.toString()); }
                br.close();
            }

            if (fileTarget.exists()) {
                const br = BufferedReader.$new(FileReader.$new(fileTarget));
                const funcAssassina = br.readLine();
                br.close();
                
                if (funcAssassina !== null) {
                    const funcName = funcAssassina.toString();
                    blacklistedFunctions.add(funcName);
                    const bw = BufferedWriter.$new(FileWriter.$new(fileBlacklist, true));
                    bw.write(JString.$new(funcName + "\n")); bw.close();
                    Java.scheduleOnMainThread(() => { Java.use("android.widget.Toast").makeText(context, JString.$new("CRASH PREVISTO: Função " + funcName.substring(0, 15) + " banida!"), 1).show(); });
                }
                fileTarget.delete();
            }
        } catch(e) {}
    }

    function registrarExecucao(funcName, mainActivity) {
        try {
            const basePath = Java.use("android.os.Environment").getExternalStorageDirectory().getAbsolutePath() + "/Android/data/" + mainActivity.getApplicationContext().getPackageName() + "/";
            const FileWriter = Java.use("java.io.FileWriter");
            const fw = FileWriter.$new(Java.use("java.io.File").$new(basePath + "alvo_atual.txt"), false);
            fw.write(Java.use("java.lang.String").$new(funcName)); fw.close();
        } catch(e) {}
    }

    function limparExecucao(mainActivity) {
        try {
            const basePath = Java.use("android.os.Environment").getExternalStorageDirectory().getAbsolutePath() + "/Android/data/" + mainActivity.getApplicationContext().getPackageName() + "/";
            const fileTarget = Java.use("java.io.File").$new(basePath + "alvo_atual.txt");
            if (fileTarget.exists()) fileTarget.delete();
        } catch(e) {}
    }

    // --- TROCA DE ÂNCORA DINÂMICA ---
    function updateScannerAnchor() {
        // Remove hooks antigos
        currentScanHooks.forEach(hook => hook.detach());
        currentScanHooks = [];
        smartListLogs = [];
        interceptedData = {};
        selectedTargetName = null;
        uiNeedsUpdate = true;

        Java.perform(() => {
            try {
                const exports = Module.enumerateExports("libqplay.so");
                exports.sort((a, b) => a.address.compare(b.address));
                let refIndex = exports.findIndex(e => e.name === currentAnchorRef);
                if (refIndex !== -1) {
                    let startIdx = Math.max(0, refIndex - 33);
                    let endIdx = Math.min(exports.length - 1, refIndex + 33);
                    for (let i = startIdx; i <= endIdx; i++) {
                        let exp = exports[i];
                        if (exp && exp.type === 'function' && exp.name !== currentAnchorRef) {
                            try {
                                let listener = Interceptor.attach(exp.address, {
                                    onEnter: function(args) {
                                        if (scanMode === 0) return;
                                        if (scanMode === 2) {
                                            if (blacklistedFunctions.has(exp.name)) return;
                                            interceptedData[exp.name] = { address: exp.address, args: [args[0], args[1], args[2], args[3]] };
                                            if (!smartListLogs.includes(exp.name)) {
                                                smartListLogs.push(exp.name);
                                                if (smartListLogs.length > 50) smartListLogs.shift();
                                                uiNeedsUpdate = true;
                                            }
                                        }
                                    }
                                });
                                currentScanHooks.push(listener);
                            } catch(e) {}
                        }
                    }
                }
            } catch(e) {}
        });
    }

    // --- LÓGICA DE SPAM ---
    function updateSpamState() {
        if (spamIntervalId) clearInterval(spamIntervalId);
        if (isSpamming) {
            spamIntervalId = setInterval(() => {
                const ap = getActivePlayer();
                if (!ap.isNull() && forceProjectile) forceProjectile(ap);
            }, spamSpeeds[spamSpeedIdx]);
        }
        if (refreshSpamUI) refreshSpamUI();
    }

    // --- HOOKS: UNHIT, WALLHACK, EXPLOSION ---
    Java.perform(() => {
        try {
            const hurtPlayer_ptr = Module.findExportByName("libqplay.so", "_ZN10W6NzgawMJy10iPzUgaQKcQEdddRK10C8THgaTQxFP10MpGzgariDy");
            if (hurtPlayer_ptr) {
                let hurtPlayer_orig = new NativeFunction(hurtPlayer_ptr, "void", ["pointer", "double", "double", "double", "pointer", "pointer"]);
                Interceptor.replace(hurtPlayer_ptr, new NativeCallback((a, b, c, d, e, f) => {
                    if (!unhit) return hurtPlayer_orig(a, b, c, d, e, f);
                    if (unhit_mode === 1) return 0; 
                    return hurtPlayer_orig(a, 0.0, 0.0, 0.0, ptr(0), ptr(0));
                }, "void", ["pointer", "double", "double", "double", "pointer", "pointer"]));
            }
        } catch(e) {}

        try {
            const wallhack_ptr = Module.findExportByName("libqplay.so", "_ZN10zF9VgaBKxR10CGk7IaA0MzEddb");
            if (wallhack_ptr) Interceptor.attach(wallhack_ptr, { 'onLeave': function(ret) { if (wallhack) ret.replace(0); } });
        } catch(e) {}

        try {
            const explode_trigger_ptr = Module.findExportByName("libqplay.so", "_ZN10zF9VgaBKxR10iUxUgaX7aQEdd");
            if(explode_trigger_ptr) Interceptor.attach(explode_trigger_ptr, { onEnter: function(args) { if (!canExplode) return; canExplode = false; makeExplosion(args[0], explosionRadius); } });
        } catch(e) {}
    });

    function makeExplosion(level, radius) { 
        if (!level || !putExplosion || !getlocalx || !getlocaly) return; 
        const activeplayer = getActivePlayer();
        if(activeplayer.isNull()) return;
        const playerX = getlocalx(activeplayer); const playerY = getlocaly(activeplayer); 
        const offset = Math.floor(radius / 2); 
        for (let i = 0; i < radius; i++) { for (let j = 0; j < radius; j++) { putExplosion(level, playerX + offset - i, playerY + offset - j, 1, 0, 1); } } 
    }

    // --- HOOK SHOW ADMINS (TAMANHO GIGANTE) ---
    setTimeout(() => {
        Java.perform(() => {
            try {
                const accRgx = /^(pc\:|graal\d+|guest.*)/;
                const adminHookPtr = Module.findExportByName("libqplay.so", "_ZN10MpGzgariDy10Q3v7IaUAWzERK10C8THgaTQxF");
                if(adminHookPtr) {
                    Interceptor.attach(adminHookPtr, { 
                        'onEnter': function(args) { 
                            const playerPtr = args[0];
                            const activeplayer = getActivePlayer();
                            if (playerPtr.toInt32() === activeplayer.toInt32()) return;
                            const props = getPlayerProperties(playerPtr);
                            const account = props.account ? props.account.toLowerCase() : '';
                            if (account !== '') { 
                                const isAdmin = !accRgx.test(account);
                                const now = Date.now();
                                if (isAdmin) { 
                                    lastAdminSeenTime = now; 
                                    if (autoOFF && !autoOffHasTriggered) { autoOffHasTriggered = true; pendingAdminDisable = true; }
                                    if (showAdmins) {
                                        if (setVarValue) {
                                            // Admin vira gigante na tela
                                            setVarValue(playerPtr, zoomAdminStr); 
                                        }
                                        if (now - lastAdminToastTime > 5000) { lastAdminToastTime = now; pendingAdminToastMsg = `ADM REVELADO: ${props.nick}`; }
                                    }
                                } 
                                // Removida a alteração de transparência dos players comuns! Ficam 100% normais.
                            }
                        } 
                    });
                }
            } catch(e) {}
        });
    }, 7000);

    // --- FUNÇÕES DE BAIXO NÍVEL ---
    function createTString(str) { if(!malloc) return ptr(0); var str_pointer = malloc(4); var str_struct = malloc(str.length + 8); str_pointer.writePointer(str_struct); str_struct.writeInt(str.length); ptr(parseInt(str_struct) + 4).writeInt(1); ptr(parseInt(str_struct) + 8).writeUtf8String(str); return str_pointer; }
    function readTString(str) { try { if (!str.isNull() && parseInt(str) != 0) { var pstr = str.readPointer(); if (!pstr.isNull()) { var strlen = pstr.readInt(); if (strlen > 0) return ptr(parseInt(pstr) + 8).readCString(strlen); } } } catch(e){} return ""; }
    function getPlayerProperties(p) { try { return { "alpha": Memory.readFloat(p.add(356)), "zoom": Memory.readFloat(p.add(0x138)), "nick": readTString(p.add(464)), "account": readTString(p.add(696)), "x": Memory.readDouble(p.add(0x238)), "y": Memory.readDouble(p.add(0x240)) }; } catch(e){ return {alpha:1, zoom:1, nick:"", account:"", x:0, y:0}; } }
    function getClassLoader() { return { Gravity: Java.use("android.view.Gravity"), TextView: Java.use("android.widget.TextView"), LinearLayout: Java.use("android.widget.LinearLayout"), LinearLayout_LayoutParams: Java.use("android.widget.LinearLayout$LayoutParams"), Color: Java.use("android.graphics.Color"), ActivityThread: Java.use("android.app.ActivityThread"), View_OnTouchListener: Java.use("android.view.View$OnTouchListener"), MotionEvent: Java.use("android.view.MotionEvent"), String: Java.use("java.lang.String"), View: Java.use("android.view.View"), DisplayMetrics: Java.use("android.util.DisplayMetrics"), ScrollView: Java.use("android.widget.ScrollView") }; }
    function getMainActivity(classLoader) { const activityThread = classLoader.ActivityThread.sCurrentActivityThread.value; return Java.cast(activityThread.mActivities.value.valueAt(0), Java.use("android.app.ActivityThread$ActivityClientRecord")).activity.value; }
    function pixelDensityToPixels(context, dp) { return parseInt(dp * context.getResources().getDisplayMetrics().density.value); }
    function showToast(context, text, time = 0) { Java.perform(() => { Java.scheduleOnMainThread(function() { Java.use("android.widget.Toast").makeText(context, Java.use("java.lang.String").$new(text), time).show(); }); }); }

    const call_antiAfk = { on() { if (!afkInterval) afkInterval = setInterval(()=>{ try { Java.perform(() => Java.scheduleOnMainThread(() => { const mainActivity = getMainActivity(getClassLoader()); let time = Java.use("android.os.SystemClock").uptimeMillis(); mainActivity.dispatchTouchEvent(Java.use("android.view.MotionEvent").obtain(time, time + 50, 0, 1, 1, 0)); })); }catch(e){} }, 240 * 1000); }, off() { if (afkInterval) { clearInterval(afkInterval); afkInterval = null; } } };
    const call_unhit_m1 = { on() { unhit = true; unhit_mode = 1; }, off() { unhit = false; } };
    const call_unhit_m2 = { on() { unhit = true; unhit_mode = 2; }, off() { unhit = false; } };
    const call_autoOFF = { on() { autoOFF = true; autoOffHasTriggered = false; }, off() { autoOFF = false; } };
    const call_showAdmins = { on() { showAdmins = true; }, off() { showAdmins = false; } };
    const call_wallhack = { on() { wallhack = true; }, off() { wallhack = false; } };
    const call_explode = { on() { if (!explosionsInterval) explosionsInterval = setInterval(() => { canExplode = true; }, 200); }, off() { if (explosionsInterval) { clearInterval(explosionsInterval); explosionsInterval = null; canExplode = false; } } };

    // --- INICIALIZAÇÃO DA UI ---
    Java.perform(function () {
        Java.scheduleOnMainThread(function () {
            try {
                const classLoader = getClassLoader();
                const mainActivity = getMainActivity(classLoader);
                const context = mainActivity.getApplicationContext();
                const Gravity = classLoader.Gravity;
                const View = classLoader.View;

                checkPreviousCrash(mainActivity);

                const COLOR_OFF = classLoader.Color.parseColor("#90000000"); 
                const COLOR_ON = classLoader.Color.parseColor("#CC008000");
                const COLOR_EXEC = classLoader.Color.parseColor("#CC9400D3"); 
                const COLOR_HEAL = classLoader.Color.parseColor("#CC008800"); 
                const COLOR_BG_PANEL = classLoader.Color.parseColor("#E6111111"); 
                const COLOR_SELECTED = classLoader.Color.parseColor("#8000FF00"); 
                
                const PADDING = pixelDensityToPixels(context, 8);
                const MARGIN = pixelDensityToPixels(context, 2);
                const btnLP = classLoader.LinearLayout_LayoutParams.$new(-2, -2);
                btnLP.setMargins(MARGIN, MARGIN, MARGIN, MARGIN);

                // --- LAYOUTS BASE ---
                const rootLayout = classLoader.LinearLayout.$new(mainActivity);
                rootLayout.setLayoutParams(classLoader.LinearLayout_LayoutParams.$new(-1, -1));
                rootLayout.setOrientation(classLoader.LinearLayout.HORIZONTAL.value);

                // COLUNA ESQUERDA (GAVETAS)
                const leftCol = classLoader.LinearLayout.$new(mainActivity);
                leftCol.setOrientation(classLoader.LinearLayout.VERTICAL.value);
                leftCol.setLayoutParams(classLoader.LinearLayout_LayoutParams.$new(-2, -1));
                leftCol.setGravity(Gravity.LEFT.value | Gravity.TOP.value);
                leftCol.setPadding(0, PADDING * 4, 0, 0);

                // COLUNA DIREITA (AÇÕES FIXAS COM MARGEM 20)
                const rightCol = classLoader.LinearLayout.$new(mainActivity);
                rightCol.setOrientation(classLoader.LinearLayout.VERTICAL.value);
                const rightLP = classLoader.LinearLayout_LayoutParams.$new(-1, -2);
                rightLP.setMargins(0, PADDING * 4, pixelDensityToPixels(context, 20), 0); // Margem de 20 pedida
                rightCol.setLayoutParams(rightLP);
                rightCol.setGravity(Gravity.RIGHT.value | Gravity.TOP.value);

                rootLayout.addView(leftCol);
                rootLayout.addView(rightCol);

                // ==========================================
                // DIREITA: BOTÕES DE AÇÃO FIXOS
                // ==========================================
                const btnExecutar = classLoader.TextView.$new(mainActivity);
                btnExecutar.setText(classLoader.String.$new("[ 🎯 EXECUTAR ]")); btnExecutar.setTextColor(classLoader.Color.WHITE.value); btnExecutar.setBackgroundColor(COLOR_EXEC); btnExecutar.setPadding(PADDING, PADDING, PADDING, PADDING); btnExecutar.setLayoutParams(btnLP);
                btnExecutar.setOnClickListener(Java.registerClass({
                    name: "ExecAction", implements: [Java.use("android.view.View$OnClickListener")],
                    methods: {
                        onClick(v) {
                            if (selectedTargetName && interceptedData[selectedTargetName]) {
                                if (blacklistedFunctions.has(selectedTargetName)) {
                                    showToast(context, "BLOQUEADO: Esta função já causou crash antes!"); return;
                                }
                                try {
                                    registrarExecucao(selectedTargetName, mainActivity);
                                    const data = interceptedData[selectedTargetName];
                                    const injectedFunc = new NativeFunction(data.address, 'void', ['pointer', 'pointer', 'pointer', 'pointer']);
                                    injectedFunc(data.args[0], data.args[1], data.args[2], data.args[3]);
                                    limparExecucao(mainActivity);
                                    showToast(context, "Ação Concluída!");
                                } catch(e) { limparExecucao(mainActivity); }
                            } else { showToast(context, "Selecione um alvo no Scanner."); }
                        }
                    }
                }).$new());

                const btnLimpar = classLoader.TextView.$new(mainActivity);
                btnLimpar.setText(classLoader.String.$new("[ LIMPAR SCAN ]")); btnLimpar.setTextColor(classLoader.Color.WHITE.value); btnLimpar.setBackgroundColor(COLOR_OFF); btnLimpar.setPadding(PADDING, PADDING, PADDING, PADDING); btnLimpar.setLayoutParams(btnLP);
                btnLimpar.setOnClickListener(Java.registerClass({
                    name: "ClearScan", implements: [Java.use("android.view.View$OnClickListener")],
                    methods: { onClick(v) { smartListLogs = []; interceptedData = {}; selectedTargetName = null; uiNeedsUpdate = true; showToast(context, "Scanner Limpo"); } }
                }).$new());

                const btnSave = classLoader.TextView.$new(mainActivity);
                btnSave.setText(classLoader.String.$new("[ SAVE TXT ]")); btnSave.setTextColor(classLoader.Color.WHITE.value); btnSave.setBackgroundColor(classLoader.Color.parseColor("#CC000088")); btnSave.setPadding(PADDING, PADDING, PADDING, PADDING); btnSave.setLayoutParams(btnLP);
                btnSave.setOnClickListener(Java.registerClass({
                    name: "SaveScan", implements: [Java.use("android.view.View$OnClickListener")],
                    methods: {
                        onClick(v) {
                            if (!selectedTargetName) return;
                            Java.perform(() => {
                                try {
                                    const filePath = Java.use("android.os.Environment").getExternalStorageDirectory().getAbsolutePath() + "/Android/data/" + context.getPackageName() + "/saved_functions.txt";
                                    let fos = Java.use("java.io.FileOutputStream").$new(filePath, true); 
                                    fos.write(Java.use("java.lang.String").$new(saveCounter + ". " + selectedTargetName + "\n").getBytes());
                                    saveCounter++; fos.close(); showToast(context, "Salvo!");
                                } catch(e) {}
                            });
                        }
                    }
                }).$new());

                rightCol.addView(btnExecutar);
                rightCol.addView(btnLimpar);
                rightCol.addView(btnSave);


                // ==========================================
                // ESQUERDA: GAVETAS MODULARES
                // ==========================================
                function createDrawer(titleText, contentLayout) {
                    const drawerContainer = classLoader.LinearLayout.$new(mainActivity);
                    drawerContainer.setOrientation(classLoader.LinearLayout.VERTICAL.value);
                    
                    const toggleBtn = classLoader.TextView.$new(mainActivity);
                    let isOpen = false;
                    toggleBtn.setText(classLoader.String.$new("[ + ] " + titleText));
                    toggleBtn.setTextColor(classLoader.Color.WHITE.value); toggleBtn.setBackgroundColor(COLOR_OFF); toggleBtn.setPadding(PADDING, PADDING, PADDING, PADDING); toggleBtn.setLayoutParams(btnLP);
                    
                    contentLayout.setVisibility(View.GONE.value);
                    contentLayout.setBackgroundColor(COLOR_BG_PANEL);
                    contentLayout.setPadding(PADDING, PADDING, PADDING, PADDING);
                    
                    toggleBtn.setOnClickListener(Java.registerClass({
                        name: "Toggle" + Math.random().toString(36).substring(2), implements: [Java.use("android.view.View$OnClickListener")],
                        methods: { onClick(v) { isOpen = !isOpen; contentLayout.setVisibility(isOpen ? View.VISIBLE.value : View.GONE.value); toggleBtn.setText(classLoader.String.$new((isOpen ? "[ - ] " : "[ + ] ") + titleText)); } }
                    }).$new());
                    
                    drawerContainer.addView(toggleBtn);
                    drawerContainer.addView(contentLayout);
                    leftCol.addView(drawerContainer);
                }

                // 1. GAVETA: FUNÇÕES CONHECIDAS (PODERES)
                const knownContent = classLoader.LinearLayout.$new(mainActivity);
                knownContent.setOrientation(classLoader.LinearLayout.VERTICAL.value);
                
                const healButton = classLoader.TextView.$new(mainActivity);
                healButton.setText(classLoader.String.$new("[ Vida Máxima ]")); healButton.setTextColor(classLoader.Color.WHITE.value); healButton.setBackgroundColor(COLOR_HEAL); healButton.setPadding(PADDING, PADDING, PADDING, PADDING); healButton.setLayoutParams(btnLP);
                healButton.setOnClickListener(Java.registerClass({ name: "HC", implements: [Java.use("android.view.View$OnClickListener")], methods: { onClick(v) { const activeplayer = getActivePlayer(); if (forceHeal && !activeplayer.isNull()) forceHeal(activeplayer, 3.0); } } }).$new());
                knownContent.addView(healButton);

                // Spam Controle
                const spamRow = classLoader.LinearLayout.$new(mainActivity);
                spamRow.setOrientation(classLoader.LinearLayout.HORIZONTAL.value);
                const spamBtn = classLoader.TextView.$new(mainActivity);
                spamBtn.setText(classLoader.String.$new("Spam Projétil: OFF")); spamBtn.setTextColor(classLoader.Color.WHITE.value); spamBtn.setBackgroundColor(COLOR_OFF); spamBtn.setPadding(PADDING, PADDING, PADDING, PADDING); spamBtn.setLayoutParams(btnLP);
                
                const speedBtn = classLoader.TextView.$new(mainActivity);
                speedBtn.setTextColor(classLoader.Color.WHITE.value); speedBtn.setBackgroundColor(classLoader.Color.TRANSPARENT.value); speedBtn.setPadding(PADDING, PADDING, PADDING, PADDING); speedBtn.setLayoutParams(btnLP);
                
                refreshSpamUI = () => {
                    Java.scheduleOnMainThread(() => {
                        spamBtn.setBackgroundColor(isSpamming ? classLoader.Color.parseColor("#CCFF8C00") : COLOR_OFF);
                        spamBtn.setText(classLoader.String.$new(isSpamming ? "Spam Projétil: ON" : "Spam Projétil: OFF"));
                        speedBtn.setText(classLoader.String.$new(spamSpeeds[spamSpeedIdx] + "ms"));
                    });
                };
                
                spamBtn.setOnClickListener(Java.registerClass({
                    name: "SpamTog", implements: [Java.use("android.view.View$OnClickListener")],
                    methods: { onClick(v) { isSpamming = !isSpamming; updateSpamState(); } }
                }).$new());

                const decSpeed = classLoader.TextView.$new(mainActivity); decSpeed.setText(classLoader.String.$new("<")); decSpeed.setTextColor(classLoader.Color.WHITE.value); decSpeed.setBackgroundColor(COLOR_OFF); decSpeed.setPadding(PADDING, PADDING, PADDING, PADDING); decSpeed.setLayoutParams(btnLP);
                decSpeed.setOnClickListener(Java.registerClass({ name: "SpamDec", implements: [Java.use("android.view.View$OnClickListener")], methods: { onClick(v) { spamSpeedIdx = Math.max(0, spamSpeedIdx - 1); updateSpamState(); } } }).$new());
                
                const incSpeed = classLoader.TextView.$new(mainActivity); incSpeed.setText(classLoader.String.$new(">")); incSpeed.setTextColor(classLoader.Color.WHITE.value); incSpeed.setBackgroundColor(COLOR_OFF); incSpeed.setPadding(PADDING, PADDING, PADDING, PADDING); incSpeed.setLayoutParams(btnLP);
                incSpeed.setOnClickListener(Java.registerClass({ name: "SpamInc", implements: [Java.use("android.view.View$OnClickListener")], methods: { onClick(v) { spamSpeedIdx = Math.min(spamSpeeds.length - 1, spamSpeedIdx + 1); updateSpamState(); } } }).$new());

                spamRow.addView(spamBtn); spamRow.addView(decSpeed); spamRow.addView(speedBtn); spamRow.addView(incSpeed);
                knownContent.addView(spamRow);
                refreshSpamUI();
                createDrawer("Funções Conhecidas", knownContent);

                // 2. GAVETA: SELETOR DE ÂNCORA
                const anchorContent = classLoader.LinearLayout.$new(mainActivity);
                anchorContent.setOrientation(classLoader.LinearLayout.VERTICAL.value);
                
                anchorOptions.forEach((opt) => {
                    const btn = classLoader.TextView.$new(mainActivity);
                    btn.setText(classLoader.String.$new(opt.name)); btn.setTextColor(classLoader.Color.WHITE.value); btn.setPadding(PADDING, PADDING, PADDING, PADDING); btn.setLayoutParams(btnLP);
                    const updateBg = () => { Java.scheduleOnMainThread(() => { btn.setBackgroundColor(currentAnchorRef === opt.ref ? COLOR_ON : COLOR_OFF); }); };
                    btn.setOnClickListener(Java.registerClass({
                        name: "Anchor" + Math.random().toString(36).substring(2), implements: [Java.use("android.view.View$OnClickListener")],
                        methods: { onClick(v) { currentAnchorRef = opt.ref; updateScannerAnchor(); showToast(context, "Âncora alterada para " + opt.name); for(let i=0; i<anchorContent.getChildCount(); i++) anchorContent.getChildAt(i).setBackgroundColor(COLOR_OFF); v.setBackgroundColor(COLOR_ON); } }
                    }).$new());
                    updateBg(); anchorContent.addView(btn);
                });
                createDrawer("Âncora do Scan", anchorContent);
                updateScannerAnchor(); // Inicia com a primeira

                // 3. GAVETA: CONSOLE SCANNER
                const scanContent = classLoader.LinearLayout.$new(mainActivity);
                scanContent.setOrientation(classLoader.LinearLayout.VERTICAL.value);
                
                const btnScanTog = classLoader.TextView.$new(mainActivity);
                btnScanTog.setText(classLoader.String.$new("INICIAR SCAN")); btnScanTog.setTextColor(classLoader.Color.WHITE.value); btnScanTog.setBackgroundColor(COLOR_OFF); btnScanTog.setPadding(PADDING, PADDING, PADDING, PADDING); btnScanTog.setLayoutParams(btnLP);
                btnScanTog.setOnClickListener(Java.registerClass({
                    name: "ScanTog", implements: [Java.use("android.view.View$OnClickListener")],
                    methods: { 
                        onClick(v) { 
                            if(scanMode === 0) { scanMode = 1; smartListLogs = []; selectedTargetName = null; uiNeedsUpdate = true; v.setBackgroundColor(COLOR_ON); setTimeout(() => { if (scanMode === 1) scanMode = 2; }, 3000); }
                            else { scanMode = 0; v.setBackgroundColor(COLOR_OFF); }
                        } 
                    }
                }).$new());
                scanContent.addView(btnScanTog);

                const scrollView = classLoader.ScrollView.$new(mainActivity);
                scrollView.setLayoutParams(classLoader.LinearLayout_LayoutParams.$new(pixelDensityToPixels(context, 250), pixelDensityToPixels(context, 200)));
                const listContainer = classLoader.LinearLayout.$new(mainActivity);
                listContainer.setOrientation(classLoader.LinearLayout.VERTICAL.value);
                scrollView.addView(listContainer);
                scanContent.addView(scrollView);

                refreshScannerUI = function() {
                    listContainer.removeAllViews();
                    if (scanMode === 1) {
                        const loadingTv = classLoader.TextView.$new(mainActivity); loadingTv.setText(classLoader.String.$new("...Calibrando (Fique Parado)...")); loadingTv.setTextColor(classLoader.Color.YELLOW.value); listContainer.addView(loadingTv);
                        return;
                    }
                    if (smartListLogs.length === 0) {
                        const emptyTv = classLoader.TextView.$new(mainActivity); emptyTv.setText(classLoader.String.$new("Vazio.")); emptyTv.setTextColor(classLoader.Color.GRAY.value); listContainer.addView(emptyTv);
                        return;
                    }
                    for (let i = 0; i < smartListLogs.length; i++) {
                        const funcName = smartListLogs[i];
                        const itemTv = classLoader.TextView.$new(mainActivity);
                        let displayName = funcName.length > 30 ? funcName.substring(0, 30) + "..." : funcName;
                        if(blacklistedFunctions.has(funcName)) displayName = "🚫 " + displayName;
                        itemTv.setText(classLoader.String.$new(displayName));
                        
                        if (funcName === selectedTargetName) { itemTv.setBackgroundColor(COLOR_SELECTED); itemTv.setTextColor(classLoader.Color.BLACK.value); } 
                        else { itemTv.setBackgroundColor(classLoader.Color.TRANSPARENT.value); itemTv.setTextColor(classLoader.Color.YELLOW.value); }
                        itemTv.setPadding(PADDING, PADDING, PADDING, PADDING);
                        
                        itemTv.setOnClickListener(Java.registerClass({
                            name: "IC"+Math.random().toString(36).substring(2), implements: [Java.use("android.view.View$OnClickListener")],
                            methods: { onClick(v) { selectedTargetName = funcName; uiNeedsUpdate = true; } }
                        }).$new());
                        listContainer.addView(itemTv);
                    }
                };
                createDrawer("Console Scanner", scanContent);

                // 4. GAVETA: MENU GERAL (Cheats antigos)
                const menuContent = classLoader.LinearLayout.$new(mainActivity);
                menuContent.setOrientation(classLoader.LinearLayout.VERTICAL.value);
                
                function createToggleButton(text, callbacks) {
                    const button = classLoader.TextView.$new(mainActivity); let isOn = false;
                    const reset = () => { if (isOn) { isOn = false; button.setBackgroundColor(COLOR_OFF); if (callbacks.off) callbacks.off(); } };
                    button.setText(classLoader.String.$new(text)); button.setTextColor(classLoader.Color.WHITE.value); button.setBackgroundColor(COLOR_OFF); button.setPadding(PADDING, PADDING, PADDING, PADDING); button.setLayoutParams(btnLP);
                    button.setOnClickListener(Java.registerClass({ name: "Tog"+Math.random().toString(36).substring(2), implements: [Java.use("android.view.View$OnClickListener")], methods: { onClick(v) { isOn = !isOn; v.setBackgroundColor(isOn ? COLOR_ON : COLOR_OFF); if (isOn) callbacks.on(); else callbacks.off(); } } }).$new());
                    return { view: button, reset: reset };
                }

                const tgUnhit = createToggleButton("Unhit(M1)", { on(){unhit=true;unhit_mode=1;}, off(){unhit=false;} });
                const tgAuto = createToggleButton("Auto Off", { on(){autoOFF=true;autoOffHasTriggered=false;}, off(){autoOFF=false;} });
                const tgRadar = createToggleButton("Radar ADM", { on(){showAdmins=true;}, off(){showAdmins=false;} });
                autoOffButtons.push(tgUnhit, tgAuto, tgRadar);
                
                menuContent.addView(tgUnhit.view); menuContent.addView(tgAuto.view); menuContent.addView(tgRadar.view);
                createDrawer("Menu Geral", menuContent);

                // INFO TEXT FIXO NO TOPO
                const topInfoTextView = classLoader.TextView.$new(mainActivity); topInfoTextView.setTextColor(classLoader.Color.WHITE.value); topInfoTextView.setShadowLayer(5, 0, 0, classLoader.Color.BLACK.value);
                leftCol.addView(topInfoTextView);

                mainActivity.addContentView(rootLayout, classLoader.LinearLayout_LayoutParams.$new(-1, -1)); 

                // ========================================================================
                // LOOP PRINCIPAL (UI e Anti-Lag)
                // ========================================================================
                setInterval(() => {
                    Java.scheduleOnMainThread(function() {
                        if (pendingAdminDisable) {
                            pendingAdminDisable = false;
                            showToast(context, 'Admin Detectado! Desativando SPAM e Cheats.');
                            for (const button of autoOffButtons) { button.reset(); }
                            // Auto-desliga o spam
                            if (isSpamming) { isSpamming = false; updateSpamState(); }
                        }
                        if (pendingAdminToastMsg) {
                            showToast(context, pendingAdminToastMsg);
                            pendingAdminToastMsg = null;
                        }
                        try {
                            const activeplayer = getActivePlayer();
                            if (!activeplayer.isNull()) {
                                const props = getPlayerProperties(activeplayer);
                                const infoText = ` Nome: ${props.nick} | Pos: (${props.x.toFixed(1)}, ${props.y.toFixed(1)})`;
                                topInfoTextView.setText(classLoader.String.$new(infoText));
                            }
                        } catch(e) {}

                        if (uiNeedsUpdate && refreshScannerUI) {
                            refreshScannerUI();
                            uiNeedsUpdate = false;
                        }
                    });
                }, 500);
                
                uiNeedsUpdate = true;
            } catch (uiError) {
                console.log("Falha de Interface: " + uiError);
            }
        });
    });
}, 8000);
