// ========================================================================
// TERMINATOR V88.13 - FORCE INJECTOR (RWX + DEEP HOOK)
// ========================================================================

let updateVirtualLog = function(msg) { console.log(msg); };

setImmediate(() => {
    Java.perform(function () {
        const ActivityThread = Java.use("android.app.ActivityThread");
        const checkReady = setInterval(() => {
            try {
                const thread = ActivityThread.sCurrentActivityThread.value;
                if (thread && thread.mActivities.value.size() > 0) {
                    clearInterval(checkReady);
                    iniciarInterfaceFuncional();
                }
            } catch (e) {}
        }, 500);
    });
});

function iniciarInterfaceFuncional() {
    Java.scheduleOnMainThread(function () {
        try {
            const ActivityThread = Java.use("android.app.ActivityThread");
            const activityThread = ActivityThread.sCurrentActivityThread.value;
            const activityClientRecord = Java.cast(activityThread.mActivities.value.valueAt(0), Java.use("android.app.ActivityThread$ActivityClientRecord"));
            const act = activityClientRecord.activity.value;
            const metrics = act.getResources().getDisplayMetrics();
            
            const cl = {
                TextView: Java.use("android.widget.TextView"),
                LinearLayout: Java.use("android.widget.LinearLayout"),
                LayoutParams: Java.use("android.widget.LinearLayout$LayoutParams"),
                FrameLayout: Java.use("android.widget.FrameLayout"),
                FLayoutParams: Java.use("android.widget.FrameLayout$LayoutParams"),
                ScrollView: Java.use("android.widget.ScrollView"),
                Color: Java.use("android.graphics.Color"),
                Touch: Java.use("android.view.View$OnTouchListener"),
                Drawable: Java.use("android.graphics.drawable.GradientDrawable")
            };

            const rootLayout = cl.FrameLayout.$new(act);
            const dragContainer = cl.LinearLayout.$new(act);
            dragContainer.setOrientation(1);
            const containerParams = cl.FLayoutParams.$new(-2, -2);
            containerParams.topMargin.value = 250; containerParams.leftMargin.value = 50;
            dragContainer.setLayoutParams(containerParams);

            const floatIcon = cl.TextView.$new(act);
            floatIcon.setText("T");
            floatIcon.setTextColor(-1);
            floatIcon.setTextSize(22);
            const iconBg = cl.Drawable.$new();
            iconBg.setShape(1); iconBg.setColor(cl.Color.parseColor("#CC000000")); iconBg.setStroke(3, cl.Color.parseColor("#00FF00"));
            floatIcon.setBackground(iconBg);
            floatIcon.setGravity(17);
            dragContainer.addView(floatIcon, cl.LayoutParams.$new(100, 100));

            const menuLayout = cl.LinearLayout.$new(act);
            menuLayout.setOrientation(1);
            const menuBg = cl.Drawable.$new();
            menuBg.setCornerRadius(20); menuBg.setColor(cl.Color.parseColor("#F2050505")); menuBg.setStroke(3, cl.Color.parseColor("#00FF00"));
            menuLayout.setBackground(menuBg);
            menuLayout.setPadding(15, 15, 15, 15);

            const logScroll = cl.ScrollView.$new(act);
            const logText = cl.TextView.$new(act);
            logText.setText("[*] Engine V88.13 Pronta...\n");
            logText.setTextColor(-1); logText.setTextSize(9);
            logScroll.addView(logText);
            menuLayout.addView(logScroll, cl.LayoutParams.$new(600, 450));

            updateVirtualLog = function(msg) {
                Java.scheduleOnMainThread(() => {
                    let lines = logText.getText().toString().split('\n');
                    if (lines.length > 20) lines.shift();
                    logText.setText(lines.join('\n') + "\n> " + msg);
                    logScroll.fullScroll(130);
                });
            };

            floatIcon.setOnTouchListener(Java.registerClass({
                name: "DragHandler_" + Math.random().toString(36).substr(2, 5),
                implements: [cl.Touch],
                methods: {
                    onTouch: function (v, event) {
                        if (event.getAction() === 1) { 
                            const vis = menuLayout.getVisibility() === 0;
                            menuLayout.setVisibility(vis ? 8 : 0);
                        }
                        return true;
                    }
                }
            }).$new());

            dragContainer.addView(menuLayout);
            rootLayout.addView(dragContainer);
            act.addContentView(rootLayout, cl.FLayoutParams.$new(-1, -1));

            setTimeout(startBrainEngine, 2000);
        } catch (e) { console.log("Erro UI: " + e); }
    });
}

function startBrainEngine() {
    const mod = Process.findModuleByName("libqplay.so");
    if (!mod) { updateVirtualLog("❌ Lib faltante"); return; }

    const OFFSETS = {
        movement: 0x002A4538, // Offset do seu dump
        walltile: 0x002B04AC  // Offset do seu dump
    };

    const addrMove = mod.base.add(OFFSETS.movement);

    // PASSO 1: FORÇAR PERMISSÃO DE ESCRITA (ESSENCIAL NO GADGET)
    Memory.protect(addrMove, 64, 'rwx');

    updateVirtualLog("📍 Base: " + mod.base);
    updateVirtualLog("🎯 Alvo: " + addrMove);

    try {
        // PASSO 2: HOOK DE BAIXO NÍVEL
        Interceptor.attach(addrMove, {
            onEnter: function(args) {
                // Se a função for chamada, isso VAI aparecer
                const id = args[1].toInt32();
                const playerPtr = args[0]; // 'this' em C++
                
                // Log de emergência no terminal do PC (mais rápido que a UI)
                console.log("[HIT] MovementAction chamada! ID: " + id);
                
                updateVirtualLog("👟 Mov: " + id + " | PTR: " + playerPtr);
            }
        });

        // Teste de chamada: Vamos ver se a lib walltile responde
        Memory.protect(mod.base.add(OFFSETS.walltile), 64, 'rwx');
        Interceptor.attach(mod.base.add(OFFSETS.walltile), {
            onEnter: function(args) {
                // Walltile costuma floodar, use com cuidado
            }
        });

        updateVirtualLog("✅ Hooks injetados com RWX!");
        updateVirtualLog("Tente se mover AGORA.");

    } catch (err) {
        updateVirtualLog("❌ Falha crítica: " + err.message);
    }
}
